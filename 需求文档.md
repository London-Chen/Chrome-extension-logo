# 浏览器插件Logo生成器 - 需求文档

## 1. 项目概述

### 1.1 项目背景
在开发浏览器插件的过程中，开发者需要准备多个不同尺寸的图标文件（16x16, 32x32, 48x48, 128x128等）。目前的AI编程工具无法直接生成图片，开发者需要：
- 寻找AI生图网站
- 手动输入提示词生成图片
- 手动调整图片尺寸
- 按规范命名每个文件

这个过程繁琐且低效。

### 1.2 项目目标
开发一个简洁的MVP版本，用户只需输入插件名称和功能描述，系统即可：
- 调用AI生图API生成图标
- 自动调整为浏览器插件所需的多尺寸规格
- 一键打包下载所有图标文件

### 1.3 目标用户
- 浏览器插件开发者
- 使用AI工具辅助开发的技术人员

---

## 2. API配置

### 2.1 生图API信息
**API提供商：** SiliconFlow
**模型：** Qwen/Qwen-Image（图片生成模型）

⚠️ **注意：** `Qwen/Qwen-Image-Edit-2509` 是图片编辑模型，不适用于从文本生成图片。

**调用示例：**
```bash
curl --request POST \
  --url https://api.siliconflow.cn/v1/images/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "Qwen/Qwen-Image",
    "prompt": "an island near sea, with seagulls, moon shining over the sea",
    "image_size": "1328x1328"
  }'
```

**API Key：** `sk-syxqdvqlysbchyfctgepefiquqjplwhdvbqnhpzwbzfsbdim`

**支持的图片尺寸（Qwen-Image模型）：**
- `1328x1328` (1:1) - 推荐用于图标生成
- `1664x928` (16:9)
- `928x1664` (9:16)
- `1472x1140` (4:3)
- `1140x1472` (3:4)

---

## 3. 功能需求（MVP版本）

### 3.1 核心功能

#### 3.1.1 用户输入
**必填字段：**
- 插件名称（例如："AdBlock Plus", "Dark Reader"）
- 插件功能描述（例如："Block advertisements on websites", "Dark mode for all websites"）

**处理逻辑：**
系统将用户输入自动构建为完整的提示词，格式如下：
```
Please help me design a browser extension icon for "[插件名称]", which is used for "[功能描述]". The icon should be simple, clear, and recognizable at small sizes. Modern flat design style with vibrant colors.
```

**示例：**
- 输入：插件名称="AdBlock", 功能描述="Block ads"
- 实际发送的prompt：
```
Please help me design a browser extension icon for "AdBlock", which is used for "Block ads". The icon should be simple, clear, and recognizable at small sizes. Modern flat design style with vibrant colors.
```

#### 3.1.2 AI图片生成
**API调用：**
- 使用 SiliconFlow 的 Qwen/Qwen-Image 模型
- 返回一张高分辨率图标（1328x1328）
- PNG格式，支持透明背景

**生成参数：**
- model: `Qwen/Qwen-Image`
- prompt: 根据用户输入自动构建的完整提示词
- image_size: `1328x1328`（Qwen-Image模型推荐的1:1比例）

#### 3.1.3 图片尺寸处理
**支持的尺寸规格：**
- 16x16 - 浏览器标签页图标（Favicon）
- 48x48 - 扩展管理页面
- 128x128 - Chrome Web Store等应用商店

**处理方式：**
- 使用前端Canvas API或后端图片处理库
- 高质量缩放算法，保持图片清晰度
- 确保在小尺寸下仍可识别核心元素

#### 3.1.4 文件命名规范
根据浏览器插件标准规范，自动命名文件：
- `icon-16.png`
- `icon-48.png`
- `icon-128.png`

#### 3.1.5 打包下载
- 用户预览生成的图标
- 点击"下载"按钮
- 自动打包为ZIP文件，包含所有三个尺寸的图标

**ZIP文件结构示例：**
```
extension-icons/
├── icon-16.png
├── icon-48.png
└── icon-128.png
```

---

## 4. 技术实现方案

### 4.1 前端技术栈（建议）
- **框架：** HTML + CSS + Vanilla JavaScript（最简方案）或 Next.js（现代方案）
- **样式：** TailwindCSS（快速开发）
- **图片处理：** Canvas API（浏览器端处理）
- **打包下载：** JSZip库

### 4.2 后端技术栈（建议）
**方案A：纯前端方案（推荐MVP）**
- 使用浏览器端Canvas API处理图片尺寸
- 使用JSZip打包下载
- 无需后端服务器，部署简单

**方案B：前后端分离**
- **后端：** Node.js + Express / Next.js API Routes
- **图片处理：** sharp.js（服务端处理，质量更好）
- **前端：** 调用后端API

### 4.3 API集成要点
```javascript
// API调用示例（前端）
const response = await fetch('https://api.siliconflow.cn/v1/images/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-syxqdvqlysbchyfctgepefiquqjplwhdvbqnhpzwbzfsbdim',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'Qwen/Qwen-Image',
    prompt: `Please help me design a browser extension icon for "${pluginName}", which is used for "${pluginDesc}". The icon should be simple, clear, and recognizable at small sizes. Modern flat design style with vibrant colors.`,
    image_size: '1328x1328'
  })
});
```

---

## 5. 用户流程（MVP）

```
1. 用户访问网站
   ↓
2. 输入插件名称和功能描述
   ↓
3. 点击"生成图标"按钮
   ↓
4. 显示加载动画（调用API生成图片）
   ↓
5. 展示生成的图标预览
   ↓
6. （可选）如果不满意，点击"重新生成"
   ↓
7. 满意后点击"下载"按钮
   ↓
8. 自动下载ZIP文件，包含所有尺寸的图标
```

---

## 6. 界面设计（MVP）

### 6.1 页面布局
```
┌─────────────────────────────────────┐
│     浏览器插件Logo生成器             │
├─────────────────────────────────────┤
│                                     │
│  插件名称：[___________________]    │
│                                     │
│  功能描述：[___________________]    │
│           [___________________]    │
│                                     │
│  [生成图标]                         │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  生成的图标预览：                   │
│  ┌─────────┐                       │
│  │         │                       │
│  │  图标   │                       │
│  │         │                       │
│  └─────────┘                       │
│                                     │
│  [重新生成]  [下载ZIP]             │
│                                     │
└─────────────────────────────────────┘
```

### 6.2 交互细节
- 输入框聚焦高亮
- 按钮悬停效果
- 生成时显示加载动画
- 错误提示友好清晰

---

## 7. MVP交付清单

### 7.1 核心功能
- [ ] 前端页面（输入表单 + 预览 + 下载）
- [ ] API集成（调用SiliconFlow生成图片）
- [ ] 图片尺寸处理（16x16, 48x48, 128x128）
- [ ] ZIP打包下载功能
- [ ] 基础UI设计

### 7.2 可选功能
- [ ] 重新生成功能
- [ ] 历史记录（本地存储）
- [ ] 响应式设计（移动端适配）

---

## 8. 技术参考代码

### 8.1 前端Canvas图片处理示例
```javascript
async function resizeImage(imageUrl, sizes) {
  const img = new Image();
  img.crossOrigin = 'anonymous';
  img.src = imageUrl;

  await new Promise((resolve) => {
    img.onload = resolve;
  });

  const results = [];

  for (const size of sizes) {
    const canvas = document.createElement('canvas');
    canvas.width = size;
    canvas.height = size;
    const ctx = canvas.getContext('2d');

    // 高质量缩放
    ctx.imageSmoothingEnabled = true;
    ctx.imageSmoothingQuality = 'high';
    ctx.drawImage(img, 0, 0, size, size);

    // 转换为Blob
    const blob = await new Promise(resolve => {
      canvas.toBlob(resolve, 'image/png');
    });

    results.push({
      size: size,
      blob: blob,
      filename: `icon-${size}.png`
    });
  }

  return results;
}
```

### 8.2 JSZip打包下载示例
```javascript
import JSZip from 'jszip';

async function downloadIcons(resizedImages) {
  const zip = new JSZip();

  // 添加所有图片到ZIP
  resizedImages.forEach(img => {
    zip.file(img.filename, img.blob);
  });

  // 生成ZIP文件
  const content = await zip.generateAsync({type: 'blob'});

  // 触发下载
  const link = document.createElement('a');
  link.href = URL.createObjectURL(content);
  link.download = 'extension-icons.zip';
  link.click();
}
```

---

## 9. 部署建议

### 9.1 快速部署方案
- **Vercel**（推荐）- 支持Next.js，免费额度
- **Netlify** - 支持静态站点
- **GitHub Pages** - 完全免费，适合纯前端方案

### 9.2 环境变量配置
```env
# .env.local
NEXT_PUBLIC_API_KEY=sk-syxqdvqlysbchyfctgepefiquqjplwhdvbqnhpzwbzfsbdim
NEXT_PUBLIC_API_URL=https://api.siliconflow.cn/v1/images/generations
```

---

**文档版本：** v2.0 (MVP精简版)
**创建日期：** 2026-01-21
**最后更新：** 2026-01-21
**更新说明：** 简化为MVP版本，使用SiliconFlow API，专注于核心功能
